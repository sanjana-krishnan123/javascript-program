

// Declaring and assigning in two steps

var playerName;
var locationName;
var score; // Step 1: Declare a score variable

playerName = "Lionel Messi";
locationName = "Argentina";
score = 100; // Step 2: Assign a value to score

console.log(playerName + " has a score of " + score);
